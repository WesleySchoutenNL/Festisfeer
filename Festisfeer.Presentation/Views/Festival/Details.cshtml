@model Festisfeer.Domain.Models.Festival

<h2 class="text-center mb-4">@Model.Name</h2>

<div class="container">
    <div class="card shadow-sm border-0">
        <img src="@Model.FestivalImg" class="card-img-top" alt="Festival afbeelding" style="height: 300px; object-fit: cover;">
        <div class="card-body">
            <h5 class="card-title">@Model.Name</h5>
            <p class="card-text"><strong>Locatie:</strong> @Model.Location</p>
            <p class="card-text"><strong>Datum:</strong> @Model.StartDateTime.ToString("yyyy-MM-dd HH:mm") - @Model.EndDateTime.ToString("yyyy-MM-dd HH:mm")</p>
            <p class="card-text"><strong>Genre:</strong> @Model.Genre</p>
            <p class="card-text"><strong>Ticketprijs:</strong> €@Model.TicketPrice</p>
        </div>
    </div>
</div>